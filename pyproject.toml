[project]
name = "copper-tools-and-armor"
version = "0.1.0"
description = "A datapack that adds copper tools, weapons, and armor with full trim support."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "beet>=0.115.0",
]
